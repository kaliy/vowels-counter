package org.kaliy.vowelscounter;

import java.util.stream.Stream;

public class VowelsCounter {
    public Stream<CountResult> countVowels(Stream<String> source) {
        return Stream.empty();
    }
}
